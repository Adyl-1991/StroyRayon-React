# Stage 38 - Production Stabilization + Build Release Readiness

Дата: 2026-07-02

## Summary

Stage 38 focused on release readiness, not new product features. The working tree changes were reviewed around product image upload, static `/uploads` serving, admin product creation, product page redesign, and empty FAQ handling.

Result: local frontend/backend checks pass, production frontend/admin page is reachable, and production API DNS is still a blocker.

## Working Tree Review

Reviewed modified files:

- `api/src/main.ts`
- `api/src/modules/admin-products/admin-products.controller.ts`
- `api/src/modules/admin-products/admin-products.service.ts`
- `src/api/client.js`
- `src/api/adminApi.js`
- `src/admin/AdminProductCreatePage.jsx`
- `src/components/product/ProductFaq.jsx`
- `src/components/product/ProductInfo.jsx`
- `src/components/product/ProductSpecs.jsx`
- `src/pages/ProductPage.jsx`
- `src/styles/admin.css`
- `src/styles/global.css`
- `scripts/admin-product-flow-qa.mjs`
- `.gitignore`

Local context/artifacts:

- `api/uploads/products/1782627758087-bb63fyf8.jpg`
- `api/uploads/products/1782809690685-lk43fyw2.png`
- `docs/STROYRAYON_PROJECT_REPORT.md`

The local upload files are valid local development artifacts, but they are not a production storage solution. `api/uploads/` is ignored by git to avoid shipping local binary upload artifacts in release commits.

## What Was Fixed

- Frontend build was rechecked and the previous Vite/Rolldown Windows `index.html` issue did not reproduce.
- Admin product flow QA was expanded to cover product image upload:
  - local PNG file selection through the admin file input;
  - frontend `FormData` upload request;
  - backend `/api/admin/products/images` response;
  - `/uploads/products/...` static serving;
  - create form URL population;
  - admin image preview rendering;
  - created product public API image persistence;
  - product page image rendering.
- QA cleanup removes only the upload file generated by the QA run.
- `api/uploads/` was added to `.gitignore` to keep local upload artifacts out of release commits.

## Commands Run

Backend:

- `npm --prefix api run build` - passed.
- `npm --prefix api test` - passed, 31/31 tests.
- `npm --prefix api run lint` - passed, no warnings reported.

Frontend:

- `npm run lint` - passed.
- `npm run build` - passed.
- `npm run qa:customer` - passed, 1762 checks, 0 failed.
- `npm run qa:admin-product-flow` - first run was blocked by occupied local Vite ports, clean rerun passed.
- `npm run qa:admin-product-flow` after Stage 38 upload QA coverage - passed, 30 checks, 0 failed.

Production probes:

- `nslookup api.stroyrayon.kg` - failed with NXDOMAIN.
- `Invoke-WebRequest https://api.stroyrayon.kg/api/health` - failed because the API host does not resolve.
- `Invoke-WebRequest https://stroy-rayon-react.vercel.app/admin/login` - returned `200`.

Product page smoke:

- Production preview route `http://127.0.0.1:4190/product/ppr-truba-pn20` returned `200`.
- Browser DOM audit passed on 1366px and 390px:
  - gallery and product info render;
  - desktop hero is side-by-side;
  - mobile layout stacks;
  - price and 2 actions are visible;
  - specs table has 16 rows;
  - FAQ renders when data exists;
  - no empty detail panels;
  - no horizontal overflow;
  - no broken images.

## Build Status

Frontend production build passes:

- `dist/index.html` emitted.
- CSS and JS bundles emitted.
- Vite reports a non-blocking large chunk warning for the main JS bundle.

Backend build passes:

- Nest build completed successfully.

## Upload Flow Status

Local upload flow is verified.

Confirmed:

- admin can select a product image file;
- frontend sends `FormData`;
- backend saves the file under `api/uploads/products`;
- backend serves it through `/uploads/products/...`;
- admin create form receives the uploaded URL;
- admin preview image renders;
- created product persists the uploaded image;
- product page renders the uploaded image without broken image errors.

Production note:

- current local disk upload storage is not release-grade for Render/Railway/Fly/VPS without a persistent disk or object storage plan.

## Product Page Status

Product page redesign is release-ready locally.

Confirmed:

- image and product info are correctly positioned on desktop/mobile;
- price, stock context, cart, and WhatsApp actions remain visible;
- characteristics render as a table;
- documentation block is conditional and hidden when no documents exist;
- FAQ is conditional and `ProductFaq` returns `null` for empty item lists;
- no decorative empty panels were found in the smoke audit.

## Production API/DNS Status

Production frontend/admin page:

- `https://stroy-rayon-react.vercel.app/admin/login` returns `200`.

Production API:

- `api.stroyrayon.kg` is still not configured in DNS.
- `https://api.stroyrayon.kg/api/health` is not reachable.

This is a real remaining production blocker, not a frontend build issue.

## Remaining Blockers

- Configure DNS for `api.stroyrayon.kg`.
- Deploy the NestJS backend to production.
- Attach production PostgreSQL and run migrations/seed/import.
- Configure `CORS_ORIGIN`, `ADMIN_JWT_SECRET`, and `VITE_API_BASE_URL`.
- Replace local upload disk storage with persistent storage or object storage before relying on production uploads.
- Decide whether to commit or archive the untracked `docs/STROYRAYON_PROJECT_REPORT.md` separately.

## Commit

Commit hash: `cf5d736`.
